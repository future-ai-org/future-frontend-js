import Logia from "../../components/Logia";

export default function LogiaPage() {
  return (
    <main style={{ marginTop: 0, paddingTop: 0 }}>
      <Logia />
    </main>
  );
}

export const dynamic = "force-dynamic";
